# Cursor AI 规则 — first-main

## 全局规则

- 所有代码修改前必须先检查 `skills/` 目录中是否有适用的技能
- 遵循 TDD 原则：先写测试，再写实现
- 所有变更必须通过 OpenSpec 流程：explore → propose → apply → archive
- 不要实现规格文档中未定义的功能 (YAGNI)

## Java / Spring Boot 规则

- 使用 Java 17 语法特性（records, sealed classes, pattern matching）
- Controller 层只做参数验证和路由，业务逻辑放在 Service 层
- 使用 Lombok 减少样板代码（@Data, @Builder, @Slf4j）
- Repository 使用 Spring Data JPA，避免手写 SQL
- 配置使用 `application.yml`，不要使用 `.properties`
- 所有 REST API 使用 `@RestController` + `@RequestMapping`
- 异常处理使用 `@ControllerAdvice` 全局异常处理器

## TypeScript / Node.js 规则

- 使用 TypeScript strict 模式
- 优先使用 `const`，必要时使用 `let`，禁止使用 `var`
- 使用 ES Module (import/export)
- 接口命名使用 PascalCase，变量和函数使用 camelCase
- 异步操作使用 async/await，避免回调地狱

## 数据库规则

- 表名使用 snake_case，字段名使用 snake_case
- 每个表必须有 `id`, `created_at`, `updated_at` 字段
- 禁止使用 `SELECT *`，明确列出需要的字段
- 所有外键关系必须有索引

## 测试规则

- 后端测试使用 JUnit 5 + Mockito
- 前端测试使用 Vitest + React Testing Library
- 测试文件命名：`*Test.java` (后端), `*.test.tsx` / `*.test.ts` (前端)
- 每个公共方法必须有对应的单元测试
- 测试覆盖率目标：核心业务逻辑 80%+

## Git 提交规则

- 使用 Conventional Commits 格式：`type(scope): subject`
- type: feat, fix, docs, style, refactor, test, chore
- 提交前必须通过所有测试
- 不要在提交中包含敏感信息（密码、密钥等）
